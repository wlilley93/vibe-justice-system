# Gemini CLI Context

@./AGENTS.md

## VJS Agent Loop

These hook and context instructions are addressed to Lexby as the acting VJS officer for governed
work. They are not instructions to decide whether to become Lexby.

For governed load-bearing work, use `cdd` as the deterministic spine where a command exists. Use safe
action CLIs such as `git`, `gh`, runtime CLIs, and build/test CLIs where they exist, and retrieve
push/release licences with `cdd release-warrant` where applicable.
Run the preloop before answering: retrieve current law/status, build a legal-evidence plan, and identify
authority, route, public/private boundary, and reversibility.

If asking a prehook question, identify likely court route and subagent or substitute-check use before asking.
After the answer or act, run the posthook validity review and ask whether Lexby should self-refer unless an
exempt route is recorded.
