id: SPEC-OPBOX-UNITARY-STACK-001
citation: "[2026] VJS-SPEC 3"
title: Opbox Unitary Stack architecture spec
purpose: >
  Define the target architecture for Opbox kernel<->frontend integration on the
  shared-Postgres single-box deployment: one source of truth per kernel-owned
  table, reads-direct, writes-via-verbs, RLS at the database; and the migration
  order that removes the transitional sync/projection. The detailed engineering
  spec (per-domain table, migration order, where-impossible carve-outs) is kept in
  the Opbox repo at kernel/docs/kernel-integration-outline.md, which gives effect
  to this decision.
scope:
  paths:
    - Executive/ministry-of-business-engineering-and-skills/engineering-department/projects/opbox/**
  action_kinds:
    - implementation_decision
    - refactor
    - schema_change
owner: principal
status: active
decisions:
  - DEC-OPBOX-UNITARY-STACK-001
invariants:
  - INV-OPBOX-UNITARY-001
obligations:
  - OBL-LOG-001
  - OBL-VALIDATE-001
review_triggers:
  - schema_changed
