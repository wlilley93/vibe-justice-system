# GitHub PAT with repo + workflow scope (never expires as long as it's used)
# Generate at: https://github.com/settings/tokens (classic, repo + workflow scopes)
GH_RUNNER_PAT=ghp_...

# Anthropic API key for the clerk's Claude calls
ANTHROPIC_API_KEY=sk-ant-...
