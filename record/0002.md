# Record 0002 — the gate binds

Verification that commencement was the only exception.

record/0001.md notes that the founding commit was made with `--no-verify`, because [2026]
VJS 5 denied it and the denial could not be satisfied at commencement. This commit is
ordinary: it touches `record/` and nothing governed, and it passes the gate on its own merits
rather than by exemption.

The gate is therefore live from commit two onward. Its three behaviours were checked
directly before founding: a hand-edited judgment is denied citing VJS 2, the same edit with
a record is allowed, and a `gate/` change is denied citing VJS 5 — each denial naming its
instrument, which upstream proves as `every_deny_names_its_law`.
