# Record 0004

[2026] VJS 6 enacted: Ruling on op:boltrig:dev-egress-loopback.

Reconstructed. `enact()` wrote this stub automatically when VJS 6 was filed, and it was
overwritten by hand-written prose before the founding commit, so the original never reached
git. This entry restates what it recorded; it does not pretend to be it.

The same defect was found and recorded in the factory's jurisdiction the day before
(its record/0027), and the test written there to prevent it — every ruling in the book keeps
an enactment stub — was left behind in that repository. Moving the test here on the court's
first day caught this within seconds.

Two lessons, and the second is the general one. A hand-written record must never take a
number `enact()` may allocate; check the book before choosing one. And a test that guards an
invariant belongs with the invariant, not with the repository that happened to discover it.

Also corrected here: `enact()` hardcoded "vps" in the prose-mirror filename, producing
`law/2026-vps-6.md` holding `[2026] VJS 6` — the law's own filename disagreeing with the law
inside it. It now reads the court code from configuration, like every other citation.
