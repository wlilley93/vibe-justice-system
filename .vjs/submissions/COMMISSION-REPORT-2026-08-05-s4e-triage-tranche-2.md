# The s4(e) triage of the unwired residue - tranche 2 (dated burndown, ACT 11 s12)

Commission work product under WARRANT-CANON-001, 2026-08-05. Four verification seats sat
concurrently over the 75 A-class candidate rows of tranche 1's mechanical pass (54
audit-machinery, 21 refusal), every seat instructed to re-execute: read the duty in its
instrument, hunt the kernel for the gate, and name the negative control - a registry row
pointing at a gate that does not enforce its duty being worse than an honest unwired row.
Verification seats ran on the economical tier per the practice direction; the Commission's
accept/reject judgment on every returned verdict was exercised in the main loop, and every
WIRE verdict was re-verified against the tree (token absent from the registry, control
present at the named path) before entry. A reclassification occurs in the audit and the
report, never in the instrument (ACT 12 s4(e)).

## The duty counts, before and after (ACT 12 s8)

- BEFORE this tranche: 399 duties, 107 wired, 292 unwired (ratchet armed at 292).
- AFTER this tranche: 399 duties, 121 wired, 278 unwired (ratchet lowered to 278 in the
  same commit - the banking rule).
- The audit remains two-state (WIRED/UNWIRED), so the reclassifications recorded below
  stay inside the 278 until the three-state machinery of ACT 11 s12 commences; the Act's
  own Schedule 2 places s12 LAST deliberately, and this tranche does not build it early.

## The fourteen rows entered (each verified: gate real, control fires)

1. record_supersession_explicitly - the warrant-register gate landed this day
   (WARRANT-SUPERSESSION-IMPLICIT/-DANGLING; control: prose_only_supersession_is_refused),
   with the honest scope note that orders carry only the weaker ORPHAN_SUPERSESSION
   existence check.
2. constituting_or_rectifying_an_apex_record_locally - the apex routing block on the
   write path itself, actor-independent (control: the subscribing-repo supreme-order
   test in hook.rs).
3. re_rendering_a_record_so_it_ceases_to_load - preserve-check refuses an unparseable
   after-file (control: an_unparseable_after_file_refuses_the_proof). Entered with its
   limitation on the row's face: a standalone s5 proof tool, not in the commit pipeline.
4. publish_this_acts_own_unwired_duties_in_the_conformance_audit - conformance_audit is
   instrument-agnostic; the ACT 11 sibling token was already credited on the same
   mechanism (control: conformance_ratchet suite).
5. record_every_digest_bump_as_a_deliberate_act - LAWPACK_LOCK_DRIFT forces the
   deliberate re-pin (control: the falsified-digest test).
6. publishing_private_facts - the Gazette publication boundary, now including the
   segment measure landed this day (control: gazette_boundary_e2e).
7. register_scheduled_machinery_as_separate_gazette_items - structurally impossible in
   code, MACHINERY kinds diverted to schedules (control: gazette_data).
8. alter_force_or_severity_of_scheduled_records_by_consolidation - schedule entries copy
   status verbatim; publication is constitutively inert (control: gazette_publication).
9. publish_v1_as_archive - the V1-archive estate with migration edges (control:
   gazette_data v1-node test).
10. load_only_law_that_hashes_to_the_pinned_digest_and_fail_closed_otherwise -
    LAWPACK_LOCK_DRIFT (control: falsified-digest test).
11. validate_mcp_input - every MCP handler refuses malformed input (control:
    unknown_method_is_refused; seeds the method class, not every param path - said on
    the row).
12. check_citation_collisions - the D2 gate (control: lawpack_resolution suite).
13. check_lawpack_digest - LAWPACK_LOCK_DRIFT again. THREE tokens now ride the one
    digest gate (with the pre-existing embedded-posture sibling, four); the registry
    block says so, so the audit never reads them as independent enforcement facts.
14. refuse_an_order_with_an_empty_issue - BUILT WITH THIS TRANCHE: the staged
    ORDER_MALFORMED empty-issue limb (ACT-PROCEEDINGS-DISCIPLINE s3), red seed and
    clean-twin control in e2e_gate_harness. An order OMITTING the field entirely fails
    the parse and rides the existing unparseable-order skip; the limb refuses the
    present-but-empty form. The absent-field class is queued below.

Three appendix rows were found ALREADY WIRED (the two s13 store-register tokens and the
subscriber-registration token): tranche 1's appendix predates the WS4 registry commit.
No action; recorded so the appendix is known stale on those rows.

## Reclassifications recorded (ACT 12 s4(e): in the audit and the report)

Agent or court-enforced conduct, no deterministic predicate:
- enumerate_every_store_in_the_named_jurisdiction and
  record_an_address_and_a_mode_on_every_finding (ACT 12 s3): the section's own
  kernel_effect excepts them on its face ("no gate holds it; enforced by the Principal
  on the closing report and by this Court on complaint").
- report_every_finding_not_rectified_with_its_reason (s8): rests on the same
  non-machine-enumerable population.
- determine_an_application_to_lift_or_narrow_a_stay_before_the_matter_it_concerns_is_entered_on_a_register
  (s6): "determine" is a judicial act; no stay concept exists in code.
- void_a_commission_act_that_reaches_a_floor (s1) and staying_an_unsatisfiable_enforcement_gate
  (s6): no Commission-act record kind exists to check, and unsatisfiability is never
  self-declared (ACT-003 s10).
- fabricate_a_pass_to_silence_the_gate (ACT-003 s10) and
  muzzle_a_gate_firing_on_a_printed_result_contradiction (ACT-003 s11): whether a
  printed pass is genuine is exactly what the kernel cannot self-verify; both sections
  make the shelter court-tested.
- reporting_a_section_of_this_act_as_enforced_before_its_gate_commences (ACT 12 s10):
  no per-section commencement model exists; the judgment is made when the registry is
  edited, reviewable on appeal.
- record_a_decisive_call_for_reversible_low_blast_work (REG-DEV-CONDUCT-001): the
  regulation keeps category-2 work deliberately ungated.

Declarative or defines-adjacent, nothing to bind:
- treating_an_enumeration_that_omits_a_store_as_a_measurement_of_the_jurisdiction
  (ACT 11 s1): a definitions-section reading rule whose teeth are the separately
  tokenised s8/s13 duties.
- require_fresh_royal_assent_to_publish_already_assented_law (REG-RELEASE-WARRANT-001),
  record_certification_as_kernel_restatement_not_attestation and
  superseding_repos_register_001 (REG-REPOS-HOUSE-001),
  treating_the_register_as_a_source_of_law (REG-REPOS-REGISTER-001): the machinery
  these describe (Repos House, the repos register) has no kernel presence at all.
- commence_a_part_2_section_only_when_its_gate_and_control_are_in_force (ACT 11 s14):
  the kernel has no Part model; commencement sequencing is legislative process.

One-time transition act:
- record_a_fresh_lock_under_a_commencement_addendum (REG-CANONICALISATION-MIGRATION-001):
  the staged migration's own addendum record is already filed.

## The build queue (named insertion points, ordered by judged value)

1. STORE-REGISTER-AWARE ALLOCATOR (ACT 11 s7): both allocators mint over the fixed
   governed-record-roots constant, never over .vjs/store-register.yaml - precisely the
   `.justice` collision blind spot s7's commentary warns about. Cheapest high-value
   build in the sweep. (cmd_next_citation, handle_allocate, live_citation_max.)
2. CLERK GATE INTO ADOPTION (REG-LAWMAKING-001 validate_before_adoption): draft check
   is real and manual only; wire it as a blocking staged check for statute/regulation
   paths. Red seed: a staged regulation with a dropped kernel_effect key passes today.
3. SELF-ASSERTED ORDINAL (ACT 11 s7): nothing checks a staged record's ordinal against
   allocator output; a hand-picked non-colliding ordinal passes.
4. Machinery-claims filing pair (ACT 11 s2); issue-tag exclusivity (s3, modelled on
   check_citation_uniqueness); directive-actor refusal and legacy count (s10);
   absent-issue field class (rides the unparseable-order skip today).
5. CONTROLS FOR EXISTING CODE: the schema_version handshake's only test bypasses the
   gate function; the pre-commit hook's proof is file-existence only - both are wired
   rows or real code whose negative control does not exercise the gate. The existing
   run_vjs_validate registry row should be re-examined on the same ground.
6. THE CROSS-REPO PRIMITIVE: nothing in the kernel reads two trees at once. Unlocks
   mirror identity (s15), both warrant dual-publication rows (ACT 12 s2), and
   warrant-in-jurisdiction checks, all of which reuse existing comparison logic
   (preserve_check::compare, the warrant parser) once a two-repo entry point exists.
7. Commission paper trail: warrant expiry/extension dates in warrants.rs (the expiry
   half of void_the_acts_of_a_dissolved_commission is a cheap extension; the voiding
   half needs a Commission-act record kind); closing-report record type; tombstone
   register (unlocks the two tombstone rows and refuse_reissue); forwarding records
   (three s7 rows); NOT-FOUND search enumeration (two s8 rows, AuthorityNotFound is
   currently dead code); reservation OWED in local-ci (s11); decision-log declarant
   (s5 - actor is hardcoded to a literal today); word-limit completeness (REG-005: two
   of ten declared classes enforced, untiered, untested); install-manifest surface
   allowlist; manifest fail-closed field validation (REG-001); negative-control
   register (s12 pair, LAST by the Act's own schedule).

## Systemic findings routed onward

- PATHSCONFIG IS PARSED AND IGNORED (REG-002 resolve_records_by_role_not_path): every
  Store read/write hardcodes .vjs/<kind> regardless of configuration - a latent breach,
  not a mere unwired duty. Routed for its own finding; the build queue carries the cure.
- Regulations describing unbuilt infrastructure (Repos House, repos register, migration
  ledger) are a different and larger class than unwired duties: a future schedule must
  BUILD the register before it can gate it.
- The registry's own commentary on word limits is stale in the safe direction (partial
  enforcement now exists that the comment denies); corrected impression carried here.
- Tranche 1's appendix is stale on three rows (above).

The ratchet stands at 278. The remaining unwired residue is 278 rows: the 34 B-class
court-enforced candidates and 3 C-class one-time acts of tranche 1 (reclassification
recorded there and here), the 183 D-awaiting-judgment rows (the next sittings' work),
and the build queue above as gates land.
