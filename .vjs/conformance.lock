# THE CONFORMANCE RATCHET (.vjs/conformance.lock).
#
# The number below is the count of enacted duties that resolve to NO gate, measured by
# `vjs audit`. `vjs validate` re-measures on every run: a HIGHER count is FATAL (new law
# landed with no gate and no recorded acceptance); a LOWER count warns until this number is
# lowered to bank the improvement.
#
# ARMED IN CANON 2026-08-05 at 295, in the same commit that lands the ratchet itself and
# the WS4 gate-registry tranche upstream (WARRANT-CANON-001 schedule, first item; the
# upstream-offer obligation of 2026-08-05). Before this commit canon measured 356 unwired
# of 399 with 43 wired; the tranche claims the 61 duty instances real gates already hold -
# gates that exist in THIS tree as of this same commit (the O5 refusal, the assent floor,
# PC-17 grounding, the lock-drift gate, the permit gate limbs, the scanner limbs, the
# bench and apex codes, the install gates, the hook word-bound, deny.toml, the
# court-forcing route block). 399 duties, 104 wired, 295 unwired. The goal from here is
# DOWN, and ACT 12 s4(e) triage under the signed warrants is the instrument that takes it
# down.
#
# LOWERED TO 292 on 2026-08-05 (WS4 tranche 2): the store-register gate lands
# (ACT-PROCEEDINGS-DISCIPLINE s13) with its register at .vjs/store-register.yaml, its
# local-ci stage, and its red seeds, wiring equal_governed_record_roots_to_the_register,
# report_an_unregistered_law_store_in_local_ci, and the respelled
# register_justice_in_the_first_subscriber_jurisdiction_at_commencement.
# 399 duties, 107 wired, 292 unwired.
#
# LOWERED TO 278 on 2026-08-05 (s4(e) triage tranche 2, four-seat sitting; report at
# .vjs/submissions/COMMISSION-REPORT-2026-08-05-s4e-triage-tranche-2.md): fourteen rows
# entered, every one re-verified against its code path AND its negative control -
# thirteen onto existing gates (warrant register, apex routing block, preserve-check,
# conformance audit, LAWPACK_LOCK_DRIFT x3, Gazette boundary + schedules + V1 estate,
# MCP door, D2 citations) and one landed with this commit (the staged empty-issue limb,
# ACT-PROCEEDINGS-DISCIPLINE s3). 399 duties, 121 wired, 278 unwired.
#
# LOWERED TO 240 on 2026-08-06 (s4(e) triage tranche 3, four-seat classification
# sitting over the 182 awaiting-judgment rows; report at
# .vjs/submissions/COMMISSION-REPORT-2026-08-06-s4e-triage-tranche-3.md): thirty-seven
# tokens entered covering thirty-eight duty rows, every one re-verified against code
# path AND negative control - the constitutional bright lines (kernel-legislates, the
# apex gate, the assent floor, the crate-closure bans, the nine-verb MCP surface), the
# permit lifecycle, the preserve-check family, the warrant cap, deterministic citation
# allocation, draft-not-binding, and the boundary scans. 399 duties, 159 wired, 240
# unwired.
unwired = 240
